# Calidad (SQA)

Planes, casos y evidencia locales del MVP.

```
calidad/
├── planes/     # plan-{slug}.md
├── casos/      # opcional
└── evidencia/  # results + screenshots
```

Ver rules `calidad-*` y skills `qa-*` en `.cursor/`.
